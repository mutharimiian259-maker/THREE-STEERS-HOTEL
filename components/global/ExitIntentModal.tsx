"use client";

/* =============================================================
   EXIT INTENT MODAL — STUB
   This component is a placeholder resolving the missing-file
   build failure. It renders nothing.

   IMPORTANT: Do NOT remove this file.
   Replace with full implementation when the feature is built.
   Rendered in app/layout.tsx with no props.
   ============================================================= */

export default function ExitIntentModal(): null {
  return null;
}
